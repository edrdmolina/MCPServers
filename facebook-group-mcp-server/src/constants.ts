import path from "path";
import os from "os";

export const DEFAULT_DATA_DIR = path.join(os.homedir(), ".facebook-group-mcp");
export const CHARACTER_LIMIT = 50000;
export const DEFAULT_SCROLL_COUNT = 50;
export const SCROLL_DELAY_MS = 2000;
export const DEFAULT_SEARCH_LIMIT = 20;
export const FACEBOOK_BASE_URL = "https://www.facebook.com";
